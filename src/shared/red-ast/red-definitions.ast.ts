export enum RedScopeDef {
  public,
  protected,
  private
}

export enum RedOriginDef {
  script,
  native,
  importonly,
}

export enum RedPrimitiveDef {
  Void,
  Bool,
  Int8,
  Uint8,
  Int16,
  Uint16,
  Int32,
  Uint32,
  Int64,
  Uint64,
  Float,
  Double,
  String,
  CName,
  ResRef,
  TweakDBID,
  Variant,
}

export enum RedTemplateDef {
  ref,
  wref,
  script_ref,
  array,
}
